export const environment = {
  production: true,
  apiUrlEmploye: 'http://springboot:8080' // Notre spring boot app tourne sur http://springboot:8080 dans le docker-compose avec springboot qui est le nom du service.
};
